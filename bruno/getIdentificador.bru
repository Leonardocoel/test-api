meta {
  name: getIdentificador
  type: http
  seq: 3
}

post {
  url: {{base_url}}/getIdentificador
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "token": "{{token}}",
    "params": {}
  }
}
